<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Edit Profile</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>

  <main class="edit-profile-container">
    <h2>Edit Your Profile</h2>

    {{#if error}}
    <p style="color:red">{{error}}</p>
    {{/if}}
    <form action="/profile-edit" method="POST">
      <label for="username">Username:</label>
      <input type="text" id="username" name="username" value="{{user.username}}" required />

      <label for="email">Email:</label>
      <input type="email" id="email" name="email" value="{{user.email}}" required />

      <label for="password">New Password:</label>
      <input type="password" id="password" name="password" placeholder="Leave blank if no change" />

      <label for="confirmPassword">Confirm Password:</label>
      <input type="password" id="confirmPassword" name="confirmPassword" placeholder="Leave blank if no change" />

      <button type="submit" class="save-button">Save Changes</button>
    </form>

    <form action="/delete-profile" method="POST" onsubmit="return confirm('Are you sure you want to delete your profile? This action cannot be undone.')">
      <button type="submit" class="delete-button">Delete Profile</button>
    </form>

    <a href="/profile" class="cancel-button">Cancel</a>
  </main>

</body>
</html>
